import 'package:flutter/material.dart';

class Px64 extends StatelessWidget {
  const Px64({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 64,
    );
  }
}
